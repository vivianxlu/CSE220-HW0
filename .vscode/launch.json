{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "Run Main",
            "program": "${workspaceRoot}/build/hw0_main",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build"
        },
    ]
}
